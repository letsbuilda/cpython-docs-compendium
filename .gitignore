# Python bytecode
__pycache__/
*.py[cod]

# Introspection run outputs — artifacts only, never committed (see CI_PLAN §3)
stdlib_api*.jsonl
official_docs_coverage_by_version.json
official_docs_gap_*.jsonl
