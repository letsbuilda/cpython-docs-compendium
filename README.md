# cpython-docs-compendium
A compendium to https://docs.python.org

The nightly `official-docs-coverage` job diffs the introspected stdlib API surface
against docs.python.org's `objects.inv` and reports the stdlib API that the official
CPython reference does not document — i.e. docs.python.org's own coverage gap.
